[project]
name = "aria"
version = "0.0.0"
description = "ARIA v0 smart glasses demo support code"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
